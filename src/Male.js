import React from 'react'

const Male = (props) => {
    return (
        <div>
            <div>{props.color}</div>
            <div>{props.height}</div>
            <div>{props.weight}</div>
        </div>
    )
}

export default Male
